import { Metadata } from 'next';
import React, { ReactNode } from 'react';

export const metadata: Metadata = {
  title: 'Debit Card Reward Calculator India 2026 | Calculate Your Rewards',
  description: 'Use our debit card reward calculator for India 2026. Estimate your cashback, points, and benefits from top debit cards. Find the best card for your spending.',
  alternates: {
    canonical: 'https://sixfinance.app/debitinfo/reward-calculator',
  },
  keywords: [
    'debit card reward calculator', 'reward calculator debit card india', 'debit card rewards calculator', 'debit card cashback calculator', 'debit card points calculator', 'debit card reward calculator 2026', 'debit card reward calculator comparison', 'debit card reward calculator review', 'debit card reward calculator eligibility', 'debit card reward calculator apply', 'debit card reward calculator online', 'debit card reward calculator instant', 'debit card reward calculator savings', 'debit card reward calculator shopping', 'debit card reward calculator travel', 'debit card reward calculator dining', 'debit card reward calculator groceries', 'debit card reward calculator fuel', 'debit card reward calculator bill payment', 'debit card reward calculator recharge', 'debit card reward calculator emi', 'debit card reward calculator wallet', 'debit card reward calculator statement', 'debit card reward calculator approval', 'debit card reward calculator best', 'debit card reward calculator guide', 'debit card reward calculator platinum', 'debit card reward calculator gold', 'debit card reward calculator silver', 'debit card reward calculator high limit', 'debit card reward calculator secure', 'debit card reward calculator insurance'
  ],
  openGraph: {
    title: 'Debit Card Reward Calculator India 2026 | Calculate Your Rewards',
    description: 'Use our debit card reward calculator for India 2026. Estimate your cashback, points, and benefits from top debit cards. Find the best card for your spending.',
    url: 'https://sixfinance.app/debitinfo/reward-calculator',
    siteName: 'SixFinance',
    type: 'website',
  },
};

export default function DebitCardRewardCalculatorLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
